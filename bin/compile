#!/usr/bin/env php
<?php
/**
 * doq phar archive compiler
 */

require __DIR__.'/../vendor/autoload.php';

use doq\Compiler;

try {
    $compiler = new Compiler();
    $compiler->compile();
} catch (\Exception $e) {
    echo 'Failed to compile phar: ['.get_class($e).'] '.$e->getMessage().' at '.$e->getFile().':'.$e->getLine().PHP_EOL;
    exit(1);
}
